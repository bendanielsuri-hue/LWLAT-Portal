# Copy to .env (gitignored) and fill in locally. See docs/adr/0012.

# Personal access token with repo/issues scope on bendanielsuri-hue/LWLAT-Portal.
# Used by the footer's "report a problem" form to file GitHub issues server-side.
GITHUB_TOKEN=

# Optional - defaults to "Development" if unset. See docs/adr/0012.
# ENVIRONMENT=Development
